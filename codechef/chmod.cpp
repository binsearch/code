#include <iostream>
#include <vector>
using namespace std;
int main(){
	int num;
	cin >> num;
	std::vector<int> arr;
	for(int i = 0; i < num; i++){
		int temp;
		cin >> temp;
		arr.push_back(temp);
	}

	int mod =
 
	for(int i = 0; i < num; i++){

	}

	int no_of_g;
	cin >> no_of_g;
}